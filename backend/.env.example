# OpenAI API Key (get from https://platform.openai.com/api-keys)
OPENAI_API_KEY=sk-proj-your-key-here

# PostgreSQL Connection String
# Format: postgres://username:password@hostname:port/database?sslmode=require
# Local example: postgres://postgres:password@localhost:5432/expenses?sslmode=disable
# Supabase example: postgres://postgres.xxx:password@aws-0-us-east-1.pooler.supabase.com:5432/postgres?sslmode=require
#
# IMPORTANT FOR NEON USERS:
# - For migrations, use the DIRECT endpoint (without -pooler): ep-xxx.us-east-1.aws.neon.tech
# - The pooler endpoint (ep-xxx-pooler) doesn't support prepared statements needed by goose
# - You can use the pooler endpoint for the running app, but use direct for migrations
DB_URL=postgres://user:password@localhost:5432/expenses?sslmode=disable

# Server Port (default: 8080)
PORT=8080
